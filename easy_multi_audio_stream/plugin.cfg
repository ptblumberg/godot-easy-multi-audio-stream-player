[plugin]

name="EasyMultiAudioStream"
description="Adds AudioPlayer nodes which allow for easily playing from multiple loaded streams using the file name as a parameter. Defaults to AudioStreamRandomizer to reduce clutter."
author="Parker Blumberg"
version="1.0"
script="plugin.gd"
